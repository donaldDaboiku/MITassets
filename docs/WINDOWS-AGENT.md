# Windows endpoint agent (separate repo/folder)

The production Windows Service lives next to this app:

`C:\Users\Donald-IT\Desktop\MSH444\MITAssetAgent\`

Not inside `MITassest/`.

- Build/install: see that folder’s `README.md` and `docs/DEPLOYMENT.md`
- This web app only shows presence (**Agents** page + Network Presence settings)
- Legacy lightweight script (optional): `agents/windows-heartbeat.ps1`
